# 老黄历择日与农事日历 · Almanac & Farm Calendar

> 类型：前端 Web 应用（含历法算法）｜难度：★★★｜建议技术栈：原生 TypeScript + Vite（零框架，练习算法）或 Vue 3 + TypeScript

## 1. 一句话简介
输入要办的事（结婚 / 搬家 / 动土 / 开业 / 安葬），按农历干支、二十四节气、宜忌推出一段日期里的可用吉日，并给出当天农事提示。

## 2. 真实场景与痛点
- 办喜事、迁坟、装修动土，长辈要「挑日子」，年轻人只会百度到一堆互相矛盾的黄历页。
- 农村种地要看节气，但手机日历不显示「芒种前后几天该干什么」。
- 农历、干支、闰月、早晚子时的换算自己是算不出来的。

## 3. 目标用户
- 要办婚丧嫁娶大事的家庭（多由年轻人代长辈查）。
- 种植户/园艺爱好者，需要节气与农事提醒。

## 4. 核心功能（MVP）
1. **日历主视图**：公历 + 农历 + 干支 + 节气 + 星期，月/周切换，闰月标注（如「闰六月」）。
2. **择日**：多选事项（嫁娶/搬家/动土/开业/出行/安葬/祭祀），选日期区间 → 输出吉日列表，含宜忌摘要与评分。
3. **日详情**：当日宜、忌、值日星神、冲煞（冲生肖 + 岁煞方位）、彭祖百忌、时辰吉凶表（十二时辰）。
4. **避讳设置**：输入家人属相/八字年柱 → 命中冲煞的日期自动排除并说明原因。
5. **农事提示**：按节气给候应与常规农事（播种/施肥/灌溉/防虫）。
6. **导出/分享**：吉日清单一键生成图片长图（适合发家庭群）。

## 5. 进阶功能
- 时辰级择日（早晚子时的日柱归属判断）。
- 「忌」重合度排序，可选重「宜」轻「忌」。
- 农历生日/祭日提醒（纯本地，无需服务端推送）。
- 手写一版简易排盘（年月日柱）用于自检。

## 6. 页面结构
```
/            日历主页（月视图 + 择日入口）
/day/:date   单日详情（宜忌 / 时辰吉凶 / 节气农事）
/pick        择日向导（事项 → 区间 → 避讳 → 结果列表）
/farm        节气农事表（一年 24 节气纵览）
```

## 7. 数据模型与算法
```ts
type LunarDay = {
  solar: string; lunar: string; ganzhi: { y: string; m: string; d: string };
  solarTerm?: string; leaps: boolean;
  yi: string[]; ji: string[]; chongShengxiao: string; shaDir: string;
};
```
- **农历换算**：内置 1900–2100 年压缩节气/闰月表（每年一个 32 位整数编码），公式化推算，不使用外部接口。
- **干支**：年柱以立春换年、月柱以节气换月、日柱用儒略日 `(JDN + 49) % 60` 推算，小时柱按早晚子时规则。
- **评分**：`score = 宜权重和 - 忌权重和 - 冲煞惩罚`，事项权重表内置可调。

## 8. 交互与视觉要点
- 宣纸质底 + 朱砂红印章风，避免俗气的满屏金色；宜/忌用两栏对仗排版。
- 吉日结果用热力色阶条（深红=最佳），点选滚动到详情。
- 全部离线可用，第一次加载即缓存年份表。

## 9. 验收标准
- 与权威万年历抽查 200 个日期（含闰月、节气交界日）零误差。
- 择日 3 年区间（约 1100 天）计算 < 300ms。
- 长图导出在手机端 1080px 宽清晰可读。

## 10. 边界（刻意不做）
不做占卜抽签等娱乐化内容，不做用户社区；只做「历法计算 + 择日检索 + 农事提示」三件事，避开黑名单里的日历打卡/习惯打卡、天气类应用。

## 11. 容器化与构建（Docker）

本项目交付**必须能通过 Docker 构建与运行**，验收一律以容器内运行结果为准。

- **Dockerfile（多阶段）**
  - `builder`：`node:20-alpine` → `npm ci` → `npm run build`，产物 `dist/`
  - `runtime`：`nginx:1.27-alpine`，仅拷贝 `dist/` 与 `nginx.conf`
- **docker-compose.yml**：服务名 `app-010`，端口 `8090:80`，`restart: unless-stopped`
- **nginx.conf**
  - SPA 回退：`try_files $uri $uri/ /index.html`
  - 内置的 1900–2100 年份表 JSON 必须开启 gzip（首屏传输 < 200KB），并长缓存 `immutable`
  - `index.html` 不缓存；正确声明 `.json` MIME
- **健康检查**：`HEALTHCHECK` 请求 `/healthz`
- **离线要求**：年份表随镜像一起打包，容器无外网时历法换算与择日仍完全可用

```bash
docker compose up -d --build
docker compose logs -f
docker compose down
```

- **验收**：`http://localhost:8090` 可用；200 个日期抽查与权威万年历零误差；3 年区间择日 < 300ms；镜像体积 < 60MB。
